apiVersion: apps/v1
kind: Deployment
metadata:
  name: critical-app
  namespace: default  # Adjust if in a different namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: critical-app
  template:
    metadata:
      labels:
        app: critical-app
    spec:
      containers:
      - name: nginx-critical-container
        image: nginx:latest
      tolerations:
      - key: "dedicated"
        operator: "Equal"
        value: "critical"
        effect: "NoSchedule"