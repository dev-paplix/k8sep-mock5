apiVersion: v1
kind: Pod
metadata:
  name: database-app
spec:
  containers:
    - name: database-app
      image: nginx
      volumeMounts:
      - mountPath: "/var/www/html"
        name: mypd
  volumes:
    - name: mypd
      persistentVolumeClaim:
        claimName: pvc-q7